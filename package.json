{
  "name": "ferrosync",
  "version": "1.0.0",
  "private": true,
  "description": "Planificación ferroviaria local de demostración",
  "scripts": {
    "start": "node server.cjs",
    "build": "node build.cjs",
    "check": "node --check app.js && node --check sw.js && node --check server.cjs && node --check test.cjs && node --check demo-operacion.cjs",
    "test": "node test.cjs",
    "demo:operacion": "node demo-operacion.cjs"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "playwright": "1.62.1" }
}
